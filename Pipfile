[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "~=2.1.0"
requests = "~=2.28.0"
pytest-cov = "~=3.0.0"

[dev-packages]
pytest = "~=7.1.0"
ipdb = "~=0.13.9"

[requires]
python_version = "3.10"

[scripts]
server = "python main.py"
tests = "pytest --cov=app tests/"
